// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// NEXT-AUTH REQUIRED MODELS
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

// CUSTOM APP MODELS

model User {
  id            String    @id @default(cuid())
  name          String?
  email         String?   @unique // Optional for Steam
  emailVerified DateTime?
  image         String?
  accounts      Account[]
  sessions      Session[]

  // Custom Competitive Stats
  steamId     String? @unique
  totalHours  Int     @default(0)
  countryCode String?

  // Customization
  mainSide       String? // "SURVIVOR", "INFECTED", "BOTH"
  survivorWeapon String? // "SMG", "SHOTGUN", "BOTH"

  communication String? // "MIC_ACTIVE", "ONLY_INFO", "LISTEN", "NO_MIC"
  skillLevel    String? // "CASUAL", "SEMI_COMP", "COMPETITIVE", "TOURNAMENT"
  bio           String? // Max 140 chars

  isPremium    Boolean @default(false)
  profileTheme String  @default("DEFAULT") // "DEFAULT", "GOLD", "DIAMOND", "RUBY", "EMERALD", "VOID"

  rating Int    @default(1000) // ELO
  rank   String @default("Unranked")
  role   String @default("Newcomer")

  wins    Int   @default(0)
  losses  Int   @default(0)
  winRate Float @default(0.0)

  // Relations
  teamId String?
  team   Team?   @relation(fields: [teamId], references: [id])

  // Matchmaking relations
  queueEntries QueueEntry[]  @relation("QueueEntries")
  matchPlayers MatchPlayer[] @relation("MatchPlayers")
  bans         Ban[]         @relation("Bans")
  banCount     Int           @default(0) // Para penalidades progresivas
  mapVotes     MapVote[]     @relation("MapVotes")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  messages Message[] @relation("Messages")
}

model Team {
  id           String  @id @default(cuid())
  name         String  @unique
  tag          String  @unique
  logoUrl      String?
  description  String?
  countryCodes String? // Comma separated ISO codes: "MX,PE,AR"
  maxMembers   Int     @default(5)

  ownerId String // Captain SteamID

  members User[]

  rating Int @default(1000)
  wins   Int @default(0)
  losses Int @default(0)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// MATCHMAKING SYSTEM

enum QueueStatus {
  WAITING
  READY_CHECK // New: Ready-up phase
  MATCHED
  ACCEPTED
  DECLINED
  TIMEOUT
}

model QueueEntry {
  id     String @id @default(cuid())
  userId String
  user   User   @relation("QueueEntries", fields: [userId], references: [id], onDelete: Cascade)

  mmr    Int // Para matchmaking
  status QueueStatus @default(WAITING)
  isReady Boolean @default(false) // Ready-up status

  matchId String?
  match   Match?  @relation(fields: [matchId], references: [id])

  createdAt DateTime @default(now())
  expiresAt DateTime // Timeout después de 30 min
}

enum MatchStatus {
  READY_CHECK // Waiting for players to ready up
  VETO // Votando mapas
  READY // Esperando conexión de jugadores
  WAITING_FOR_PLAYERS // Server ready, waiting for players to connect and ready up
  IN_PROGRESS // Partida en curso
  PAUSED
  COMPLETED
  CANCELLED
}

model Match {
  id     String      @id @default(cuid())
  status MatchStatus @default(VETO)

  // Servidor
  serverId       String?
  server         GameServer? @relation(fields: [serverId], references: [id])
  serverIp       String?
  serverPort     Int?
  serverPassword String?

  // Jugadores (8 total)
  players      MatchPlayer[]
  queueEntries QueueEntry[]

  // Mapa seleccionado
  selectedMap String?
  mapVotes    MapVote[]

  // Resultados
  teamAScore Int     @default(0)
  teamBScore Int     @default(0)
  winnerTeam String? // "TEAM_A" or "TEAM_B"

  rounds Round[]

  // Timestamps
  createdAt    DateTime  @default(now())
  startedAt    DateTime?
  completedAt  DateTime?
  
  // Additional fields
  cancelReason String?
  mapName      String?
}

model MatchPlayer {
  id      String @id @default(cuid())
  matchId String
  match   Match  @relation(fields: [matchId], references: [id], onDelete: Cascade)

  userId String
  user   User   @relation("MatchPlayers", fields: [userId], references: [id], onDelete: Cascade)

  team      String  @default("TEAM_A") // "TEAM_A" or "TEAM_B"
  hasJoined Boolean @default(false) // Tracked when player connects to server
  accepted  Boolean @default(false) // Ready-check acceptance

  // ELO tracking
  eloAtStart Int?  // ELO when match started
  eloAtEnd   Int?  // ELO after match completed
  eloChange  Int?  // Change in ELO (+15, -20, etc.)

  // Stats
  kills     Int @default(0)
  deaths    Int @default(0)
  damage    Int @default(0)
  headshots Int @default(0)

  // Disconnect tracking
  disconnectedAt   DateTime?
  disconnectReason String?

  roundStats PlayerRoundStats[]

  @@unique([matchId, userId])
}

model MapVote {
  id      String @id @default(cuid())
  matchId String
  match   Match  @relation(fields: [matchId], references: [id], onDelete: Cascade)

  userId String
  user   User   @relation("MapVotes", fields: [userId], references: [id], onDelete: Cascade)

  map String // "c1m1_hotel", "c2m1_highway", etc.

  createdAt DateTime @default(now())

  @@unique([matchId, userId]) // Un voto por jugador
}

model Round {
  id      String @id @default(cuid())
  matchId String
  match   Match  @relation(fields: [matchId], references: [id], onDelete: Cascade)

  roundNumber Int    // 1, 2, 3, 4, etc.
  map         String // Map name

  // Score breakdown from Scoremod
  teamScore    Int @default(0) // Total score for this round
  healthBonus  Int @default(0) // Health bonus component
  damageBonus  Int @default(0) // Damage bonus component
  pillsBonus   Int @default(0) // Pills bonus component
  
  // MVP for this round
  mvpSteamId String?

  playerStats PlayerRoundStats[]

  startedAt DateTime  @default(now())
  endedAt   DateTime?
}

model PlayerRoundStats {
  id      String @id @default(cuid())
  roundId String
  round   Round  @relation(fields: [roundId], references: [id], onDelete: Cascade)

  matchPlayerId String
  matchPlayer   MatchPlayer @relation(fields: [matchPlayerId], references: [id], onDelete: Cascade)

  steamId String // For easy reference

  // Stats from Survivor MVP plugin
  kills     Int @default(0) // SI kills
  damage    Int @default(0) // Total damage
  common    Int @default(0) // Common infected kills
  
  // Legacy stats
  deaths    Int @default(0)
  headshots Int @default(0)
}

// BAN SYSTEM

enum BanReason {
  AFK_ACCEPT // No aceptó partida
  NO_JOIN // No se unió al servidor
  CRASH // Se desconectó durante partida
  MANUAL // Ban manual por admin
}

model Ban {
  id     String @id @default(cuid())
  userId String
  user   User   @relation("Bans", fields: [userId], references: [id], onDelete: Cascade)

  reason   BanReason
  duration Int // Minutos

  matchId String? // Match relacionado (si aplica)

  createdAt DateTime @default(now())
  expiresAt DateTime
  active    Boolean  @default(true)
}

// GAME SERVERS

enum ServerStatus {
  AVAILABLE
  IN_USE
  OFFLINE
  MAINTENANCE
}

model GameServer {
  id           String @id @default(cuid())
  name         String
  ipAddress    String
  port         Int
  rconPassword String?
  serverKey    String @unique // For API authentication
  isActive     Boolean @default(true)

  status         ServerStatus @default(AVAILABLE)
  currentMatches Match[]

  createdAt DateTime @default(now())
}

// CHAT SYSTEM

model Message {
  id        String   @id @default(cuid())
  content   String
  createdAt DateTime @default(now())

  userId String
  user   User   @relation("Messages", fields: [userId], references: [id], onDelete: Cascade)
}
